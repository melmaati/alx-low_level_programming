#include <stdio.h>
/**
* main -  main block
* Description: print all single digit numbers of base 10
* Return: Always 0 (Success)
*/
int main(void)
{
int sum;

for (sum = 0; sum < 10; sum++)
printf("%d", sum);

printf("\n");

return (0);
}
